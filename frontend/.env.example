VITE_API_BASE=http://localhost:8787

# Optional. Leave unset to run in open mode (no login, original pose-check + one-off
# yoga-plan form — everything built before accounts/schedules). Set it to enable
# Clerk login, onboarding, and the personalized weekly schedule.
# VITE_CLERK_PUBLISHABLE_KEY=your_clerk_publishable_key_here
